import html from './categories.html?raw';

export const Categories = () => {
    // Si tuvieras lógica compleja de TS que quisieras inyectar antes,
    // podrías manipular la variable 'html' aquí.
    return html;
}